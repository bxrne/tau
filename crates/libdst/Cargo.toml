[package]
name = "libdst"
version = "0.3.0" # x-release-please-version
edition.workspace = true
license.workspace = true
description = "Deterministic simulation testing framework for dual-model verification"
publish = false

[lib]
name = "libdst"
path = "src/lib.rs"

[dependencies]
rand = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
hegeltest = { workspace = true }
hegeltest-macros = { workspace = true }
tempfile = { workspace = true }
